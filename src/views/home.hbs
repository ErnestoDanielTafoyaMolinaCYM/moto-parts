<h1>Catalogo de partes</h1>
<div class="container">
    <div class="row">
        {{#each parts}}
            <div class="col-md-4 mb-3">
                {{> part-card}}
            </div>
        {{/each}}
    </div>
</div>